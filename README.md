```bash
# ESLint
$ yarn add -D eslint@6.8.0
$ yarn add -D eslint-plugin-react@^7.19.0 @typescript-eslint/eslint-plugin@latest
$ yarn add -D eslint-config-airbnb@latest eslint-plugin-import@^2.20.1
$ yarn add -D eslint-plugin-jsx-a11y@^6.2.3 eslint-plugin-react-hooks@^2.5.0
$ yarn add -D @typescript-eslint/parser@latest
$ yarn add -D eslint-import-resolver-typescript
# Prettier
$ yarn add -D prettier eslint-config-prettier eslint-plugin-prettier
```
```bash
$ yarn eslint --init
```
```bash
$ yarn add react-router-dom
$ yarn add -D @types/react-router-dom
```
```bash
$ yarn add styled-components
$ yarn add -D @types/styled-components
```
